'use strict'

const fib = require('./ab-solution-2')

/**
 * Write a function fib() that a takes an integer n and returns the nth
 * fibonacci number.
 *
 * Let's say our fibonacci series is 0-indexed and starts with 0. So:
 *
 *   fib(0); // => 0
 *   fib(1); // => 1
 *   fib(2); // => 1
 *   fib(3); // => 2
 *   fib(4); // => 3
 *   ...
 */

console.log('fib(0)', fib(0))    // 0
console.log('fib(1)', fib(1))    // 1
console.log('fib(2)', fib(2))    // 1
console.log('fib(3)', fib(3))    // 2
console.log('fib(4)', fib(4))    // 3
